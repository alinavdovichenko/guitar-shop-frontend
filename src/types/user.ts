export type UserData = {
  email: string;
  token: string;
};

export type User = {
  name: string;
  email: string;
  password: string;
};

export type UserConnect = {
  email: string;
  token: string;
  name: string;
};
